function Header({ title }) {
  return (
    <header style={{ marginBottom: "20px" }}>
      <h1>{title}</h1>
      <p>Learning reusable components using props</p>
    </header>
  );
}

export default Header;
